<div class="blog-list">
    <ul>
        {{#each model as |blog|}}
        <li>
            <span>{{blog.date}}</span>
            {{#link-to 'blog' blog.id class="blog-title"}}{{blog.title}}{{/link-to}}
        </li>
        {{/each}}
    </ul>
</div>